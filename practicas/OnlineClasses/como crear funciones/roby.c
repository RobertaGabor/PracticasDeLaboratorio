#include "roby.h"

int sumarV1(int a,int b)
{
    int rta;
    rta=a+b;

    return rta;//se puede llamar igual que una var en main porque estan en funciones distintas igual

    /* o SE PUEDE HACER SI SOLO PONEMOS RETURN A+B*/
}
